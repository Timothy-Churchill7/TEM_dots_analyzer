services:
  - type: web
    name: tem-dot-analyzer
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn tem_webapp:app --timeout 120
    envVars:
      - key: SECRET_KEY
        generateValue: true
